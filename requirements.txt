# pygame-ce provides `import pygame` and ships wheels for newer Python (e.g. 3.14).
# On Raspberry Pi you can use `pygame` instead if you prefer stock wheels from pip/apt.
pygame-ce>=2.5.7
gpiozero>=2.0.1
# Raspberry Pi: install lgpio for Python via apt (not pip — pip often builds from source and
# needs swig + dev headers). See documentation/set-up-hat.md § Python GPIO.
cairosvg>=2.7.0
ijson>=3.3.0
