# The Momir Machine

This project contains all the code, setup, and components I used to buld my Momir Machine

## Running tests

Install dependencies:

`python -m pip install -r requirements-dev.txt`

Run the full suite:

`python -m pytest`